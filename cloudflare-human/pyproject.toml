[project]
name = "buildawallet-cloudflare"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["fastapi>=0.115"]

[dependency-groups]
dev = ["workers-py", "workers-runtime-sdk", "pytest", "httpx"]
